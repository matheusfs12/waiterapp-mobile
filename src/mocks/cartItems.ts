import { CartItem } from '../types/CartItem';
import { products } from './products';

export const cartItemsList: CartItem[] = [
    {
        product: products[0],
        quantity: 1,
    },
    {
        product: products[1],
        quantity: 22,
    },

];
